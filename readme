{
    "code": "def example_function():\n    this_is_a_very_long_variable_name = 'example'\n    print('Hello, world!')\n\n# This line exceeds the maximum line length to trigger the enforcement\nsome_long_variable_name = 'This is a very long string that exceeds the maximum line length to trigger the enforcement'"
}
